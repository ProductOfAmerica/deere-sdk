# api-surface.yaml
#
# Operation identity to public method name registry. Each entry maps one API
# operation, keyed by (HTTP method, normalized path), to the public method name
# the SDK generator emits. The generator never invents or rebinds a name: a
# name lives in this file or the operation has no name yet.
#
# Path-param names normally do not affect identity (GET /orgs/{orgId} and
# GET /orgs/{organizationId} are the same operation), so an upstream param
# rename is absorbed and the public method is preserved. The exception is
# sibling operations that differ ONLY by param name: crop-types declares both
# GET /cropTypes/{name} and GET /cropTypes/{id} as distinct operations. Those
# are matched by exact path, so a param rename within such a sibling set is not
# absorbed; the old entry goes missing and the rename surfaces as a breaking
# diagnostic for a human to reconcile.
#
# This file is REGENERATED by generate-sdk whenever new upstream operations
# appear, so it is rewritten wholesale. Per-entry hand comments are not
# preserved; only the "name:" values are meant to be hand-edited.
#
# Breaking-change runbook:
#   Upstream renamed a path: update that entry's "op:" to the new path. The
#     name stays, so the public method is preserved.
#   Upstream removed an endpoint: delete the entry. That drops the public
#     method, which is a major-version consideration.
#
# Proposed names for NEW operations preserve interior camel humps (for example
# "listMeasurementTypes", not "listMeasurementtypes"), unlike the historical
# names seeded from the legacy generator.
#
# "listAll" never appears here: it is a derived twin of any collection GET
# named "list", emitted automatically by the generator.
#
version: 1
specs:
  aemp:
    - op: GET /Fleet/{pageNumber}
      name: get
  assets:
    - op: DELETE /assets/{assetId}
      name: delete
    - op: GET /assetCatalog
      name: getAssetcatalog
    - op: GET /assets/{assetId}
      name: get
    - op: GET /assets/{assetId}/locations
      name: listLocations
    - op: GET /organizations/{orgId}/assets
      name: list
    - op: POST /assets/{assetId}/locations
      name: createLocations
    - op: POST /organizations/{orgId}/assets
      name: create
    - op: PUT /assets/{assetId}
      name: update
  boundaries:
    - op: DELETE /organizations/{orgId}/fields/{fieldId}/boundaries/{boundaryId}
      name: delete
    - op: GET /fieldOperations/{operationId}/boundary
      name: get
    - op: GET /organizations/{orgId}/boundaries
      name: list
    - op: GET /organizations/{orgId}/fields/{fieldId}/boundaries
      name: listBoundaries
    - op: GET /organizations/{orgId}/fields/{fieldId}/boundaries/{boundaryId}
      name: getBoundaries
    - op: POST /organizations/{orgId}/fields/{fieldId}/boundaries
      name: create
    - op: PUT /organizations/{orgId}/fields/{fieldId}/boundaries/{boundaryId}
      name: update
  clients:
    - op: DELETE /organizations/{orgId}/clients/{clientId}
      name: delete
    - op: GET /organizations/{orgId}/clients
      name: list
    - op: GET /organizations/{orgId}/clients/{clientId}
      name: get
    - op: GET /organizations/{orgId}/clients/{id}/farms
      name: listFarms
    - op: GET /organizations/{orgID}/clients/{id}/fields
      name: listFields
    - op: POST /organizations/{orgId}/clients
      name: create
    - op: PUT /organizations/{orgId}/clients/{clientId}
      name: update
  connection-management:
    - op: DELETE /connections/{connectionId}
      name: delete
    - op: DELETE /organizations/{orgId}/connections
      name: deleteConnections
    - op: GET /connections
      name: list
  crop-types:
    - op: GET /cropTypes
      name: list
    - op: GET /cropTypes/{id}
      name: getCroptypes
    - op: GET /cropTypes/{name}
      name: get
    - op: GET /organizations/{organizationId}/cropTypes
      name: listCroptypes
  equipment:
    - op: DELETE /equipment/{id}
      name: delete
    - op: GET /equipment
      name: get
    - op: GET /equipment/{id}
      name: getEquipment
    - op: GET /equipmentISGTypes
      name: listEquipmentisgtypes
    - op: GET /equipmentMakes
      name: list
    - op: GET /equipmentMakes/{equipmentMakeId}
      name: getEquipmentmakes
    - op: GET /equipmentMakes/{equipmentMakeId}/equipmentISGTypes
      name: getEquipmentisgtypes
    - op: GET /equipmentMakes/{equipmentMakeId}/equipmentISGTypes/{equipmentISGTypeId}
      name: getEquipmentisgtypes2
    - op: GET /equipmentMakes/{equipmentMakeId}/equipmentISGTypes/{equipmentISGTypeId}/equipmentModels
      name: getEquipmentmodels
    - op: GET /equipmentMakes/{equipmentMakeId}/equipmentISGTypes/{equipmentISGTypeId}/equipmentModels/{equipmentModelId}
      name: getEquipmentmodels2
    - op: GET /equipmentMakes/{equipmentMakeId}/equipmentTypes
      name: getEquipmenttypes
    - op: GET /equipmentModels
      name: listEquipmentmodels
    - op: GET /equipmentTypes
      name: listEquipmenttypes
    - op: POST /organizations/{organizationId}/equipment
      name: create
    - op: PUT /equipment/{id}
      name: update
  equipment-measurement:
    - op: POST /organizations/{organizationId}/equipment/{principalId}/measurements
      name: create
  farms:
    - op: DELETE /organizations/{orgId}/farms/{farmId}
      name: delete
    - op: GET /organizations/{orgId}/farms
      name: list
    - op: GET /organizations/{orgId}/farms/{farmId}
      name: get
    - op: GET /organizations/{orgId}/farms/{farmId}/clients
      name: listClients
    - op: GET /organizations/{orgID}/farms/{id}/fields
      name: listFields
    - op: POST /organizations/{orgId}/farms
      name: create
    - op: PUT /organizations/{orgId}/farms/{farmId}
      name: update
  field-operations-api:
    - op: GET /fieldOperations/{operationId}
      name: get
    - op: GET /fieldOps/{operationId}
      name: getFieldops
    - op: GET /organizations/{orgId}/fields/{fieldId}/fieldOperations
      name: list
  fields:
    - op: DELETE /organizations/{orgId}/fields/{fieldId}
      name: delete
    - op: GET /organizations/{orgId}/fields
      name: list
    - op: GET /organizations/{orgId}/fields/{fieldId}
      name: get
    - op: GET /organizations/{orgID}/fields/{id}/clients
      name: listClients
    - op: GET /organizations/{orgId}/fields/{fieldId}/farms
      name: listFarms
    - op: POST /organizations/{orgId}/fields
      name: create
    - op: PUT /organizations/{orgId}/fields/{fieldId}
      name: update
  files:
    - op: GET /files
      name: list
    - op: GET /files/{fileId}
      name: get
    - op: GET /organizations/{orgId}/files
      name: listFiles
    - op: POST /organizations/{orgId}/files
      name: create
    - op: PUT /files/{fileId}
      name: update
  flags:
    - op: DELETE /organizations/{orgId}/flags/{flagId}
      name: delete
    - op: GET /organizations/{orgId}/fields/{fieldId}/flags
      name: list
    - op: GET /organizations/{orgId}/flags
      name: getFlags
    - op: GET /organizations/{orgId}/flags/{flagId}
      name: get
    - op: POST /organizations/{orgId}/flags
      name: create
    - op: PUT /organizations/{orgId}/flags/{flagId}
      name: update
  guidance-lines:
    - op: GET /organizations/{orgId}/fields/{fieldId}/guidanceLines
      name: list
    - op: GET /organizations/{orgId}/fields/{fieldId}/guidanceLines/{guidanceLineId}
      name: get
    - op: POST /organizations/{orgId}/fields/{fieldId}/guidanceLines
      name: create
    - op: PUT /organizations/{orgId}/fields/{fieldId}/guidanceLines/{guidanceLineId}
      name: update
  harvest-id:
    - op: GET /organizations/{orgId}/harvestIdentificationModules
      name: list
    - op: GET /organizations/{orgId}/harvestIdentificationModules/{serialNumber}
      name: get
  machine-alerts:
    - op: GET /machines/{principalId}/alerts
      name: list
  machine-device-state-reports:
    - op: GET /machines/{principalId}/deviceStateReports
      name: get
  machine-engine-hours:
    - op: GET /machines/{principalId}/engineHours
      name: list
  machine-hours-of-operation:
    - op: GET /machines/{principalId}/hoursOfOperation
      name: list
  machine-locations:
    - op: GET /machines/{principalId}/locationHistory
      name: get
  map-layers:
    - op: DELETE /mapLayerSummaries/{id}
      name: delete
    - op: GET /mapLayerSummaries/{id}
      name: get
    - op: GET /organizations/{orgId}/fields/{id}/mapLayerSummaries
      name: list
    - op: POST /organizations/{orgId}/fields/{id}/mapLayerSummaries
      name: create
  notifications:
    - op: DELETE /notificationEvents/{sourceEvent}
      name: delete
    - op: GET /notifications/{sourceEvent}
      name: get
    - op: GET /organizations/{orgId}/notifications/events
      name: list
    - op: POST /notificationEvents
      name: create
  operators:
    - op: DELETE /organizations/{orgId}/operators
      name: delete
    - op: DELETE /organizations/{orgId}/operators/{id}
      name: deleteOperators
    - op: GET /organizations/{orgId}/operators
      name: list
    - op: GET /organizations/{orgId}/operators/{id}
      name: get
    - op: POST /organizations/{orgId}/operators
      name: create
    - op: PUT /organizations/{orgId}/operators/{id}
      name: update
  organizations:
    - op: GET /organizations
      name: list
    - op: GET /organizations/{orgId}
      name: get
    - op: GET /organizations/{orgId}/users
      name: listUsers
    - op: GET /users/{userName}/organizations
      name: listOrganizations
  partnerships:
    - op: DELETE /partnerships/{token}
      name: delete
    - op: GET /partnerships
      name: list
    - op: GET /partnerships/{token}
      name: get
    - op: GET /partnerships/{token}/permissions
      name: listPermissions
    - op: POST /partnerships
      name: create
    - op: POST /partnerships/{token}/permissions
      name: createPermissions
  products:
    - op: GET /organizations/{organizationId}/varieties
      name: list
    - op: GET /organizations/{organizationId}/varieties/{erid}
      name: get
    - op: GET /varieties
      name: listVarieties
    - op: GET /varieties/{erid}
      name: getVarieties
    - op: GET /varieties/{erid}/documents
      name: listDocuments
    - op: PATCH /varieties/{erid}/setOverridesForOrg/{organizationId}
      name: patch
    - op: POST /organizations/{organizationId}/varieties
      name: create
    - op: POST /varieties/{erid}/associateToOrg/{organizationId}
      name: createAssociatetoorg
    - op: PUT /organizations/{organizationId}/varieties/{erid}
      name: update
  users:
    - op: GET /users/{username}
      name: get
  webhook:
    - op: GET /eventSubscriptions
      name: list
    - op: GET /eventSubscriptions/{id}
      name: get
    - op: POST /eventSubscriptions
      name: create
    - op: PUT /eventSubscriptions/{id}
      name: update
